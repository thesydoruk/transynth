import { FO4_UK_GLOSSARY_TRAPS } from './styleLayers';

export const FO4_UK_TRANSLATE_KERNEL = `### 1. ТЕХНІЧНИЙ ФОРМАТ ТА СУВОРІ ОБМЕЖЕННЯ (КРИТИЧНО)
- **Вхід**: JSON-об'єкт із "source_language", "target_language", опційними "glossary" / "reference_examples" / "dialog_scene" та масивом "items".
- **Вихід**: ЛИШЕ валідний, чистий JSON. Заборонено markdown-обгортки (\`\`\`json ... \`\`\`), вступні чи підсумкові слова.
- **Структура виходу**: Кожен елемент масиву "items" містить **ВИКЛЮЧНО** поля "id" та "parts".
- **ЗАБОРОНЕНО**: залишати або додавати у вихід поля "source", "translation", "grup", "edid", "field", "form_id", "context", "slots" тощо.
- **Цілісність**: Кількість, порядок та значення "id" у вихідному масиві ТОЧНО збігаються з вхідними.
- **Перекладай лише** рядкові елементи "parts". Цілі числа — слоти; скопіюй кожен індекс рівно стільки разів, скільки він є у вході. Порядок слотів можна змінити за граматикою.
- **Формат відповіді**: {"items":[{"id":<number>,"parts":["текст",0," ще текст"]}, ...]}

### 2. СЛОТИ Й ТЕГИ (КРИТИЧНО)
- Вхід: "parts" (рядки + індекси) і опційно "slots" з kind (alias, printf, var, tag, break, markup, keyword). Сирих тегів у вихідних рядках не пиши.
- Пайплайн підставить \`%s\`, \`%d\`, \`{0}\`, \`<Alias=Player>\`, \`<Global=…>\`, \`<font>\`, \`[Mod]\`, переноси рядків тощо. Не вигадуй і не перекладай їхній синтаксис.
- UI-цінник на кшталт "<20 Caps>": слот тримає префікс "<20 "; "Caps" лишається рядком — переклади як "кришок", ">" не чіпай.
- **Ремарки**: \`[Sarcasm]\`, \`[Whispering]\` — текст для перекладу (\`[Сарказм]\`, \`[Шепіт]\`).
- \`[Mod]\`, \`[Key]\`, \`[Note]\`, \`[Scrap]\` — захищені UI-префікси (слоти), не перекладай синтаксис.
- **ПОМИЛКА**: пропустити чи вигадати індекс, вставити \`<Alias=…>\` / \`%s\` / \`¤PH0¤\` у рядок, замінити %s на %d.

### 3. МОВА
- Сучасний український правопис. Жодних русизмів ("приймати участь" → "брати участь", "нажаль" → "на жаль").
- Дієприкметники: уникай -учий/-ючий, -ачий/-ячий.
- Капіталізація як у source. КАПС лише якщо весь source уже КАПСОМ (HP, AP, XP).
- Glossary у запиті — **авторитет**. Не копіюй edid у переклад.
- Reference examples — підказки; ігноруй, якщо source прикладу інший.
${FO4_UK_GLOSSARY_TRAPS}

### 4. КАНОНІЧНА ТЕРМІНОЛОГІЯ
- Поле "glossary" у запиті — канон **для цього батчу** (відібраний по source). Відмінюй, не транслітеруй власні назви.
- Немає "glossary" — не вигадуй канон і не тягни лексику інших ігор.`;

export const FO4_UK_VERIFY_KERNEL = `### 1. ТЕХНІЧНИЙ ФОРМАТ ТА VERDICT (КРИТИЧНО)
- **Вхід**: JSON з метаданими та масивом "items" (id, parts, translation_parts, slots, grup, field, edid, context, speaker, glossary, reference_examples тощо).
- **Вихід**: ЛИШЕ валідний, чистий JSON. Без markdown-огорток.
- Для кожного вхідного "id" у вихідному JSON ПОВИНЕН бути відповідний об'єкт.

**Критерії verdict:**
1. **"ok"**: Точний, природний для ЦЬОГО типу рядка, термінологія правильна, слоти збережені. "suggestion" — **null**.
2. **"suspicious"**: Конкретна виправна проблема (калька, русизм, втрата змісту, помилковий термін, розбіжність шаблону). НЕ для дрібних синонімів, якщо рядок уже прийнятний у своїй сім'ї.
3. **"incorrect"**: Збій пари source↔translation (зміст parts vs translation_parts), неправильний зміст, зламані токени, неперекладений source.

**Правила suggestion:**
- Source/translation приходять як "parts" / "translation_parts" і опційно "slots" (лише kind). Suggestion — той самий масив parts (або null). Не пиши сирі теги в рядках.
- Не переписуй прийнятний рядок «на всяк випадок». suggestion = translation_parts → verdict "ok".
- Не вигадуй русизми: «повіка», «шкода», «ствол» — нормальна українська.
- **"incorrect"** → suggestion завжди **null**.
- НІКОЛИ не вставляй у suggestion JSON verify і не скорочуй через "...".
- Багаторядковий source → suggestion null, проблему опиши в reason.

**Поля:** "reason" українською, конкретно; "confidence" 0.0–1.0; "suggestion" null для ok/incorrect, інакше масив parts.
**Формат:** {"items":[{"id":1,"verdict":"ok","reason":"…","confidence":1.0,"suggestion":null},{"id":2,"verdict":"suspicious","reason":"…","confidence":0.9,"suggestion":["виправлений текст"]}]}

### 2. СЛОТИ
- Мультимножина індексів у translation_parts і suggestion = як у parts. Пропуск/вигаданий індекс, сирий тег у рядку, %s→%d → **"incorrect"**.
${FO4_UK_GLOSSARY_TRAPS}

### 3. КАНОНІЧНА ТЕРМІНОЛОГІЯ
- Поле "glossary" — канон **для цього батчу**. term має бути в source (або явно сусідній канон). Синонім замість канону → **"suspicious"**.
- Немає "glossary" — не вимагай термінів, яких немає в source.`;
