# Keeps editors from writing CRLF back into files that .gitattributes stores as
# LF. Prettier also reads this file, so formatting and Git agree.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
