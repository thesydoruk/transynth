# 04 — Гарячі клавіші

Працюйте в редакторі швидше за допомогою гарячих клавіш.
Усі shortcuts активні, коли сторінка редактора у фокусі.

---

## Зміст

- [Комбінації редактора](#комбінації-редактора)
- [Комбінації навігації](#комбінації-навігації)
- [Комбінації статусів](#комбінації-статусів)
- [Поради щодо використання гарячих клавіш](#поради-щодо-використання-гарячих-клавіш)

---

## Комбінації редактора

| Комбінація   | Дія                                                        |
| ------------ | ---------------------------------------------------------- |
| `Ctrl+S`     | Зберегти поточний переклад                                 |
| `Ctrl+Enter` | Зберегти з translation textarea у Detail Panel             |
| `Enter`      | Перевести фокус у translation textarea для активного рядка |
| `Escape`     | Закрити context menu, Detail Panel або скинути selection   |
| `?`          | Увімкнути/вимкнути overlay з довідкою по гарячих клавішах  |

Важливі поправки:

- Поточний редактор **не** використовує inline-редагування grid-cell, тому `Enter` не підтверджує inline edit.
- Поточний редактор **не** має shortcut `Ctrl+H` для Search & Replace.
- Search & Replace відкривається кнопкою на toolbar.

---

## Комбінації навігації

| Комбінація              | Дія                                                   |
| ----------------------- | ----------------------------------------------------- |
| `↑` / `↓`               | Перемістити selection на один рядок вгору/вниз        |
| `Page Up` / `Page Down` | Перейти на попередню / наступну сторінку              |
| `N`                     | Перейти до наступного порожнього (untranslated) рядка |
| `Ctrl+Shift+E`          | Відкрити або закрити Detail Panel                     |

Деталі навігації:

- Навігація стрілками циклічно переходить через верх і низ поточної сторінки.
- `N` шукає вперед від поточного рядка і циклічно рухається, доки не знайде наступний рядок без перекладу.
- `Page Up` і `Page Down` лише змінюють сторінку; page size залишається фіксованим на 100 рядків.
- Якщо Detail Panel закрита, `Ctrl+Shift+E` відкриє її на першому видимому рядку поточної сторінки.

---

## Комбінації статусів

| Комбінація     | Дія                                            |
| -------------- | ---------------------------------------------- |
| `Ctrl+Shift+A` | Погодити активний рядок                        |
| `Ctrl+Shift+R` | Відхилити активний рядок                       |
| `Ctrl+Shift+C` | Скопіювати source text у translation field     |
| `Ctrl+Shift+X` | Очистити поточний переклад                     |
| `Space`        | Перемкнути selection для активного рядка       |
| `Ctrl+A`       | Вибрати або зняти вибір з усіх рядків сторінки |

Важливі поправки:

- `Ctrl+Shift+R` відхиляє активний переклад; він **не** скидає рядок у порожній стан.
- `Ctrl+Shift+C` копіює source text; він **не** застосовує TM suggestion.
- `Ctrl+Shift+X` очищує translation; він **не** запускає LLM translation.
- `Ctrl+Shift+E` перемикає Detail Panel; він **не** відкриває ESP Explorer.
- `Space` перемикає selection рядка; він **не** перемикає approval state.

Поточне обмеження:

- Вбудовані keyboard handlers працюють з **активним рядком**, а не з усім multi-selection, якщо йдеться про approve/reject/copy/clear.
- Bulk-операції для кількох selected rows доступні через toolbar і context menu, а не через окремі keyboard shortcuts.

Також зараз немає documented shortcuts на кшталт `Ctrl+D` для Draft або окремого shortcut для відкриття Search & Replace.

---

## Поради щодо використання гарячих клавіш

- **Більшість shortcuts працює для поточного selection.** Якщо вибрано кілька рядків, batch-actions доступні переважно через toolbar і context menu.
- **Ctrl+S** зберігає одразу, не залишаючи поле — це зручно після voice typing або вставки з іншого джерела.
- Якщо shortcut не спрацьовує, спочатку клікніть будь-де в grid редактора, щоб сторінка отримала keyboard focus.

Уточнення для поточної реалізації:

- Global keyboard handlers ігнорують більшість shortcuts, поки ви друкуєте в `input`, `select` або `textarea`, крім явно дозволених дій на кшталт `Escape`, `Ctrl+S` і `Ctrl+Shift+...`.
- Multi-row batch actions передусім винесені в toolbar і context menu. Shortcuts на кшталт `Ctrl+Shift+A` усе одно працюють з активним рядком.
- Якщо browser або його extensions перехоплюють shortcut на кшталт `Ctrl+S`, використайте чистий browser profile або вимкніть конфліктні extensions для застосунку.

Примітка для modal-вікон:

- **Book / HTML Editor** modal також підтримує `Escape` для закриття і `Ctrl+Enter` для збереження.

---

← [Редактор](03-editor.md) | [Головна](README.md) | **Далі: [Пам’ять перекладів →](05-translation-memory.md)**
